node_modules
dist
coverage
*.local
.DS_Store
.vite
*.tsbuildinfo

# Secrets — never commit these
.env

# Runtime session data (JSON state file / Railway volume)
# Anchored to the repo root so it never matches src/data/.
/data
/server/data
